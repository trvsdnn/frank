- DONE - support layouts of a different type than the rendered template
dynamically reload template changes using javascript and a slick url
dynamically reload helpers and settings
support yaml variable definition at the top of templates
add helper for "selected" css class, this should work with frankup and frankout
add frankout replacement fields to lorem helpers 